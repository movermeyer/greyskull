language: python
python:
    - "3.2"
    - "3.3"
    - "3.4"
install:
    - pip install .
    - pip install coveralls
script: nosetests --with-coverage --cover-package=greyskull
after_success: coveralls